{
  "name": "cursor-cli-acp",
  "version": "0.1.0",
  "description": "ACP adapter for cursor-agent CLI",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "cursor-cli-acp": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsdown src/index.ts --format esm --target node20 --out-dir dist --clean",
    "dev": "tsx src/index.ts"
  },
  "dependencies": {
    "@agentclientprotocol/sdk": "^0.13.0"
  },
  "devDependencies": {
    "@types/node": "^22",
    "tsdown": "^0.15.0",
    "tsx": "^4",
    "typescript": "^5"
  }
}
